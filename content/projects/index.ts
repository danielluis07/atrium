import { lyngen } from "@/content/projects/lyngen";
import type { Project } from "@/content/schema";

/** The Project order: the Project Index and the next-Project wrap both follow it. */
export const projectOrder: Project[] = [lyngen];
